[build]
  base = "frontend"
  command = "cd .. && npm install --production && cd frontend && npm install && npm run build"
  publish = "dist"
  functions = "../netlify/functions"

[functions]
  node_bundler = "esbuild"
  included_files = ["../backend/**"]

# Redirigir /api/metrics/* a la Netlify Function
[[redirects]]
  from = "/api/metrics/*"
  to = "/.netlify/functions/metrics/:splat"
  status = 200

# Redirigir /api/meta/* a la Netlify Function de Meta
[[redirects]]
  from = "/api/meta/campaigns"
  to = "/.netlify/functions/meta"
  status = 200

# Redirigir /api/ai/* a la Netlify Function de AI
[[redirects]]
  from = "/api/ai/analyze"
  to = "/.netlify/functions/ai"
  status = 200

# SPA fallback - cualquier otra ruta va al index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
